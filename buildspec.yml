phases:
  pre_build:
    commands:
      - pip install -r backend/requirements.txt
  build:
    commands:
      - python -m unittest discover -s my-api
artifacts:
  files:
    - '**/*'
  base-directory: my-api
